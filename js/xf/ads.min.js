"use strict";

var _createClass = function () { function defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if ("value" in descriptor) descriptor.writable = true; Object.defineProperty(target, descriptor.key, descriptor); } } return function (Constructor, protoProps, staticProps) { if (protoProps) defineProperties(Constructor.prototype, protoProps); if (staticProps) defineProperties(Constructor, staticProps); return Constructor; }; }();

function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); } }

/*-----------------------------------------------------------------------------------------------------------------------------------------------------------------------
 1. 项目名称：悟空找房h5
 2. 页面名称：xf/ads(新房-广告)
 3. 作者：zhaohuagang@lifang.com
 -----------------------------------------------------------------------------------------------------------------------------------------------------------------------*/
var XfAds = function () {
    function XfAds() {
        _classCallCheck(this, XfAds);

        /*-----------------------------------------------------------------------------------------------------------------------------------------------------------------------
        定义常量
        -----------------------------------------------------------------------------------------------------------------------------------------------------------------------*/
        this.consts = {
            "$adsContainer": $(".xf-ads"),
            "$xfItemsContainer": $(".container"),
            "$conningTower": $(".conning-tower"),
            "duration": 100
        };
        /*-----------------------------------------------------------------------------------------------------------------------------------------------------------------------
        实例化swiper
        -----------------------------------------------------------------------------------------------------------------------------------------------------------------------*/
        require(["../components/swiper-3.4.2.jquery.min"], function () {
            var pictAds = new Swiper(".pict-ads", {
                autoplay: 4000,
                pagination: ".swiper-pagination",
                paginationClickable: true
            });
            var textAds = new Swiper(".text-ads .items", {
                autoplay: 3000,
                direction: "vertical"
            });
        });
        /*-----------------------------------------------------------------------------------------------------------------------------------------------------------------------
        添加事件监听
        -----------------------------------------------------------------------------------------------------------------------------------------------------------------------*/
        this.addListener();
        /*-----------------------------------------------------------------------------------------------------------------------------------------------------------------------
        页面加载的时候就触发一下window的scroll来根据滚动条来判断广告是否显示
        -----------------------------------------------------------------------------------------------------------------------------------------------------------------------*/
        $(window).trigger("scroll");
    }
    /*-----------------------------------------------------------------------------------------------------------------------------------------------------------------------
    添加事件监听
    -----------------------------------------------------------------------------------------------------------------------------------------------------------------------*/


    _createClass(XfAds, [{
        key: "addListener",
        value: function addListener() {
            var _this = this;

            $(window).on("scroll", function () {
                var st = $(window).scrollTop();
                if (st <= 50 && !_this.consts.$adsContainer.is(":visible")) {
                    _this.consts.$adsContainer.slideDown(_this.consts.duration);
                    _this.consts.$conningTower.css({ "padding-bottom": "10px" });
                    _this.consts.$xfItemsContainer.animate({ paddingTop: "313px" }, _this.consts.duration);
                } else if (st > 50 && _this.consts.$adsContainer.is(":visible")) {
                    _this.consts.$adsContainer.slideUp(_this.consts.duration);
                    _this.consts.$conningTower.css({ "padding-bottom": 0 });
                    _this.consts.$xfItemsContainer.animate({ paddingTop: 97 + st + "px" }, _this.consts.duration);
                }
            });
        }
    }]);

    return XfAds;
}();

/*-----------------------------------------------------------------------------------------------------------------------------------------------------------------------
类的初始化
-----------------------------------------------------------------------------------------------------------------------------------------------------------------------*/


$(document).ready(function () {
    new XfAds();
});
//# sourceMappingURL=ads.min.js.map
